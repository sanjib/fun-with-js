/**
 * Ordered!
 *
 * Array Big O Properties
 * Insert
 *      Head    O(n)    (shift, unshift)
 *      Tail    O(1)    (push, pop)
 * Remove
 *      Head    O(n)    (shift, unshift)
 *      Tail    O(1)    (push, pop)
 * Search   O(n)
 * Access   O(1)
 */

let names = ["Michael", "Melissa", "Andrea"];
let values = [true, {}, [], 2, "awesome"];

// O(1)
// push, pop, 

// O(n)
// shift, unshift, concat, slice, splice
// foreach, map, filter, reduce

// O(n Log n)
// sort

